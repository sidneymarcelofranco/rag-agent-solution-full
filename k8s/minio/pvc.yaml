apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rag-minio-pvc
  namespace: rag-agent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 100Gi
